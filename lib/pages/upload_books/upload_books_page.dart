import 'package:flutter/material.dart';

class UploadBooksPage extends StatelessWidget {
  const UploadBooksPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
